{
  "name": "tyov-companion",
  "version": "2.0.0",
  "description": "Thousand Year Old Vampire — a no-build, offline-capable companion PWA for the solo journaling RPG.",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "python3 -m http.server 8000",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
